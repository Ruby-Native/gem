source "https://rubygems.org"

gemspec

group :development, :test do
  # Parses HTML and ERB together, for `ruby_native check`. Rails 8.2 and later
  # ship it as an Action View dependency, so most apps already have it.
  gem "herb"
  gem "minitest"
  gem "rake"
  gem "sqlite3"
end
